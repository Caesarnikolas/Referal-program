<div class="container">
  <div class="row row-cols-2">
    <div class="col">
      <img src="/uploads/{{user.photo}}" class="img-fluid" alt="...">
    </div>
    <div class="col" name="user-name">
      Имя пользователя : {{user.name}}
    </div>
    <div class="col" name="user-phone">
      Phone: {{user.phone}}
    </div>
    <div class="col" name="user-mail">
      Email:{{user.email}}
    </div>
    <a href="/admin/edit/{{user._id}}">
      <button class='header-button' id="editButton" type="submit">Редактировать</button>
    </a>
  </div>
</div>
